# 《二次元萌宠具身智能系统研发》论文数学与算法层面改进建议

本文档基于当前初稿 `初稿.docx` 编写，目标是增强论文中的数学建模、算法表达、控制公式和实验评价指标，使论文从“系统实现说明”进一步提升为“具有明确算法设计和工程评价的毕业设计论文”。

## 一、总体修改思路

当前初稿的优点是系统结构完整，工程实现细节充分，能够说明系统确实完成了语音唤醒、视觉问答、TTS播放、前端表情和舵机控制等功能。主要不足是算法层面的形式化表达偏少，缺少公式、伪代码、状态机和量化指标定义。

建议不要大量展开 Transformer、Qwen-VL 或 ASR 模型内部原理，因为这些模型并非本文训练或改进的对象。更合适的做法是围绕本文实际实现的系统流程进行建模，包括：

1. 语音唤醒门控算法；
2. 视觉上下文触发函数；
3. 表情状态机；
4. 人脸检测与跟踪误差建模；
5. 舵机角度控制律；
6. PCA9685 PWM 映射；
7. 系统性能评价指标。

这些内容都与当前系统实现直接对应，答辩时也更容易解释。

## 二、建议调整后的章节标题

建议对第三章和第四章部分标题进行轻微调整，使其更突出算法和设计方法。

| 原章节标题 | 建议章节标题 |
|---|---|
| 3.3 语音唤醒与对话流程设计 | 3.3 基于时间窗口的语音唤醒门控算法 |
| 3.4 视觉上下文采集与触发策略设计 | 3.4 基于意图关键词的视觉上下文触发算法 |
| 3.5 表情生成机制与本地音频播放设计 | 3.5 表情状态机与音频同步机制设计 |
| 3.7 人脸追踪算法设计 | 3.7 基于DNN检测与MIL跟踪的人脸追踪算法设计 |
| 3.8 PCA9685舵机控制与安全策略 | 3.8 舵机角度映射与PCA9685安全控制策略 |
| 4.5 系统性能指标测试 | 4.5 系统性能评价指标与测试结果 |

## 三、第二章可补充的数学基础

第二章不宜写得过重，建议只补充与系统实现直接相关的基础公式。

### 2.2 语音活动检测可补充内容

建议插入位置：第二章 `2.2 语音识别、语音活动检测与语音合成` 中介绍 VAD 后。

可插入文本：

> 语音活动检测的核心目标是从连续音频帧中判断当前片段是否包含有效人声。设第 \(t\) 帧音频特征为 \(a_t\)，VAD 模型输出该帧属于语音的概率为：
>
> \[
> p_t=P(y_t=1|a_t)
> \]
>
> 其中 \(y_t=1\) 表示当前帧为语音帧，\(y_t=0\) 表示当前帧为静音或噪声帧。当 \(p_t\) 大于设定阈值 \(\tau\) 时，系统认为当前帧存在语音活动：
>
> \[
> y_t=
> \begin{cases}
> 1, & p_t \ge \tau \\
> 0, & p_t < \tau
> \end{cases}
> \]
>
> 在实际系统中，阈值设置会影响误触发率和漏检率。阈值过低时，环境噪声或扬声器播放的AI语音容易被误识别为用户输入；阈值过高时，短句或较轻的语音可能被忽略。因此本文结合前端自动停止麦克风和后端会话状态复位机制，共同降低误触发对交互流程的影响。

### 2.4 PCA9685 PWM 基础公式

建议插入位置：第二章 `2.4 树莓派、摄像头与PCA9685舵机控制` 中介绍 PCA9685 后。

可插入文本：

> PCA9685 内部采用 12 位计数器产生 PWM 信号。在舵机控制中，舵机角度通常由 PWM 高电平脉宽决定。设 PWM 频率为 \(f\)，目标脉宽为 \(p\)，单位为微秒，则一个周期对应的时间为：
>
> \[
> T=\frac{1}{f}
> \]
>
> 由于 PCA9685 将一个周期划分为 4096 个计数单位，目标脉宽对应的计数值可表示为：
>
> \[
> N=\frac{p \cdot f \cdot 4096}{10^6}
> \]
>
> 其中 \(N\) 为写入 PWM 寄存器的计数值。通过改变 \(p\) 的大小即可改变舵机目标角度。

## 四、第三章重点增强内容

第三章是最适合补充公式和算法的章节。建议把语音、视觉、表情、人脸追踪和舵机控制都形式化。

### 3.3 基于时间窗口的语音唤醒门控算法

建议插入位置：当前 `3.3 语音唤醒与对话流程设计` 的自然语言描述之后。

可插入文本：

> 为降低环境噪声和非目标语音导致的误触发，本文将语音交互过程建模为带时间窗口的二状态门控模型。设客户端在时刻 \(t\) 的语音状态为：
>
> \[
> S(t)\in \{S_0,S_1\}
> \]
>
> 其中，\(S_0\) 表示未唤醒状态，\(S_1\) 表示已唤醒活跃状态。设唤醒词集合为：
>
> \[
> W=\{w_1,w_2,\cdots,w_n\}
> \]
>
> ASR 转写文本为 \(x\)，则唤醒词匹配函数定义为：
>
> \[
> g(x)=
> \begin{cases}
> 1, & \exists w_i \in W,\ w_i \subset x \\
> 0, & otherwise
> \end{cases}
> \]
>
> 设当前活跃截止时间为 \(T_d\)，活跃窗口长度为 \(\Delta T\)，本文系统中 \(\Delta T=20s\)。语音是否进入正常对话流程可表示为：
>
> \[
> P(x,t)=
> \begin{cases}
> 1, & t<T_d \\
> 1, & g(x)=1 \land r(x)\neq \varnothing \\
> 0, & otherwise
> \end{cases}
> \]
>
> 其中 \(r(x)\) 表示去除唤醒词后的有效问题文本。当 \(g(x)=1\) 且 \(r(x)=\varnothing\) 时，系统只返回唤醒确认语“我在”，并将活跃截止时间更新为：
>
> \[
> T_d=t+\Delta T
> \]
>
> 该模型使系统既能在唤醒后支持连续对话，又能在长时间无交互后自动回到未唤醒状态。

建议加入伪代码：

```text
算法3-1 基于时间窗口的语音唤醒门控算法

输入：ASR转写文本x，当前时间t，唤醒词集合W，活跃截止时间Td，窗口长度ΔT
输出：是否进入对话流程process，用户有效输入u

1. 对x进行文本清洗，去除空白、标点和常见语气词
2. if t < Td then
3.     process = True
4.     u = x
5.     Td = t + ΔT
6.     return process, u, Td
7. end if
8. 在W中匹配x包含的唤醒词
9. if 未匹配唤醒词 then
10.    process = False
11.    发送conversation-chain-end信号复位前端状态
12.    return process, null, Td
13. end if
14. 从x中去除唤醒词，得到剩余文本r(x)
15. Td = t + ΔT
16. if r(x)为空 then
17.    生成唤醒确认回复“我在”
18.    process = False
19.    return process, null, Td
20. else
21.    process = True
22.    u = r(x)
23.    return process, u, Td
24. end if
```

### 3.4 基于意图关键词的视觉上下文触发算法

建议插入位置：当前 `3.4 视觉上下文采集与触发策略设计` 中关键词说明之后。

可插入文本：

> 为避免每轮对话都上传图像造成额外的网络和模型推理开销，本文设计了轻量化视觉意图触发函数。设用户输入文本为 \(x\)，视觉意图关键词集合为：
>
> \[
> K=\{k_1,k_2,\cdots,k_m\}
> \]
>
> 视觉上下文触发函数定义为：
>
> \[
> V(x)=
> \begin{cases}
> 1, & \exists k_i \in K,\ k_i \subset x \\
> 0, & otherwise
> \end{cases}
> \]
>
> 当 \(V(x)=1\) 时，系统读取后端摄像头缓存的最新帧 \(img_t\)，并将其与文本问题共同构造成多模态模型输入：
>
> \[
> I=
> \begin{cases}
> \{x,img_t\}, & V(x)=1 \\
> \{x\}, & V(x)=0
> \end{cases}
> \]
>
> 该策略将视觉模型调用限制在“当前画面”“你看得见我吗”“我手里拿着什么”等明确视觉问题中，降低了树莓派终端的带宽压力和云端模型调用成本。

建议加入伪代码：

```text
算法3-2 视觉上下文触发与输入构造算法

输入：用户文本x，视觉关键词集合K，摄像头最新帧缓存F
输出：模型输入I

1. trigger = False
2. for 每个关键词k in K do
3.     if k出现在x中 then
4.         trigger = True
5.         break
6.     end if
7. end for
8. if trigger = True and F不为空 then
9.     将最新JPEG帧编码为base64图像img
10.    I = {文本x，图像img}
11. else
12.    I = {文本x}
13. end if
14. return I
```

### 3.5 表情状态机与音频同步机制设计

建议插入位置：当前 `表情生成机制与本地音频播放设计` 中表情映射表之前。

可插入文本：

> 本文将前端表情变化建模为有限状态机。有限状态机定义为：
>
> \[
> M=(Q,\Sigma,\delta,q_0)
> \]
>
> 其中，\(Q\) 为表情状态集合，\(\Sigma\) 为输入事件集合，\(\delta\) 为状态转移函数，\(q_0\) 为初始状态。本文系统中的状态集合定义为：
>
> \[
> Q=\{idle,thinking,neutral,happy,sad,angry,surprised\}
> \]
>
> 输入事件集合包括后端对话事件和模型输出情绪标签：
>
> \[
> \Sigma=\{start,end,joy,smirk,sadness,anger,surprise,fear\}
> \]
>
> 状态转移过程可表示为：
>
> \[
> q_{t+1}=\delta(q_t,e_t)
> \]
>
> 其中 \(q_t\) 表示当前表情状态，\(e_t\) 表示当前接收到的后端事件或情绪标签。例如：
>
> \[
> \delta(q_t,start)=thinking
> \]
>
> \[
> \delta(q_t,joy)=happy
> \]
>
> \[
> \delta(q_t,sadness)=sad
> \]
>
> \[
> \delta(q_t,anger)=angry
> \]
>
> \[
> \delta(q_t,end)=neutral
> \]
>
> 通过该状态机，前端显示层不需要依赖具体 Live2D 模型，也可以根据后端事件稳定地呈现对话状态和情绪反馈。

### 3.7 基于DNN检测与MIL跟踪的人脸追踪算法设计

建议插入位置：当前 `3.7 人脸追踪算法设计` 中“目标水平误差定义为...”之后。

可插入文本：

> 设摄像头图像宽度为 \(W\)，高度为 \(H\)，DNN 检测或 MIL 跟踪得到的人脸边界框为：
>
> \[
> B_t=(x_t,y_t,w_t,h_t)
> \]
>
> 其中 \((x_t,y_t)\) 为边界框左上角坐标，\(w_t\) 和 \(h_t\) 分别为边界框宽度和高度。人脸中心横坐标为：
>
> \[
> c_t=x_t+\frac{w_t}{2}
> \]
>
> 图像中心横坐标为：
>
> \[
> c_0=\frac{W}{2}
> \]
>
> 为消除不同分辨率下误差尺度差异，本文将人脸水平偏移归一化为：
>
> \[
> e_t=\frac{c_t-c_0}{W/2}
> \]
>
> 其中 \(e_t\in[-1,1]\)。当 \(e_t<0\) 时表示目标偏向画面左侧，当 \(e_t>0\) 时表示目标偏向画面右侧。
>
> 为减小单帧检测噪声对舵机动作的影响，系统对误差进行指数平滑：
>
> \[
> \hat{e}_t=\alpha e_t+(1-\alpha)\hat{e}_{t-1}
> \]
>
> 其中 \(\alpha\in(0,1]\) 为平滑系数。较大的 \(\alpha\) 能提高响应速度，但也会增加抖动；较小的 \(\alpha\) 能提高稳定性，但会降低跟随速度。
>
> 为避免目标接近画面中心时舵机频繁微小摆动，系统设置死区阈值 \(d\)：
>
> \[
> e'_t=
> \begin{cases}
> 0, & |\hat{e}_t|<d \\
> \hat{e}_t, & |\hat{e}_t|\ge d
> \end{cases}
> \]
>
> 其中 \(d\) 对应系统配置中的 `dead_zone`。经过死区处理后的误差 \(e'_t\) 再进入舵机控制模块。

建议加入伪代码：

```text
算法3-3 DNN检测与MIL跟踪融合的人脸追踪算法

输入：摄像头当前帧frame，当前跟踪器tracker，最大跟踪帧数N
输出：归一化水平误差e

1. if tracker不存在 or 连续跟踪帧数超过N then
2.     将frame缩放并转换为DNN输入blob
3.     使用DNN人脸检测模型得到候选框集合B
4.     筛选置信度高于阈值的候选框
5.     if 存在有效候选框 then
6.         选择面积最大的候选框作为目标框
7.         使用目标框初始化MIL跟踪器
8.     else
9.         返回无目标状态
10.    end if
11. else
12.    使用MIL跟踪器更新目标框
13.    if 跟踪失败 then
14.        清空tracker，返回无目标状态
15.    end if
16. end if
17. 根据目标框中心计算归一化水平误差e
18. 对e进行指数平滑和死区处理
19. return e
```

### 3.8 舵机角度映射与PCA9685安全控制策略

建议插入位置：当前 `3.8 PCA9685舵机控制与安全策略` 中介绍 `kp、dead_zone、max_step` 后。

可插入文本：

> 根据人脸追踪模块输出的水平误差 \(e'_t\)，舵机控制模块采用比例控制方式计算角度修正量：
>
> \[
> \Delta \theta_t=clamp(k_p e'_t,-s_{max},s_{max})
> \]
>
> 其中 \(k_p\) 为比例系数，\(s_{max}\) 为单次控制周期允许的最大角度变化。函数 \(clamp(\cdot)\) 用于限制输出范围，避免舵机在单帧误差较大时产生过快动作。
>
> 舵机目标角度更新为：
>
> \[
> \theta_t=clamp(\theta_{t-1}+\Delta \theta_t,\theta_{min},\theta_{max})
> \]
>
> 其中 \(\theta_{min}\) 和 \(\theta_{max}\) 分别表示舵机允许的最小和最大安全角度。通过比例控制、死区和最大步长限制，系统可以在跟随目标的同时降低机械抖动和过冲风险。
>
> 舵机角度到 PWM 脉宽的映射采用线性关系。设舵机角度范围为 \([\theta_{min},\theta_{max}]\)，脉宽范围为 \([p_{min},p_{max}]\)，则目标角度 \(\theta_t\) 对应的脉宽为：
>
> \[
> p(\theta_t)=p_{min}+\frac{\theta_t-\theta_{min}}{\theta_{max}-\theta_{min}}(p_{max}-p_{min})
> \]
>
> 结合 PCA9685 的 12 位计数器，可进一步计算写入 PWM 寄存器的计数值：
>
> \[
> N_t=\frac{p(\theta_t)\cdot f \cdot 4096}{10^6}
> \]
>
> 其中 \(f\) 为 PWM 频率，\(p(\theta_t)\) 单位为微秒。

## 五、第四章评价指标增强

第四章已经有性能测试表，但建议补充“指标定义”，使实验更规范。

### 4.5 系统性能评价指标与测试结果

建议插入位置：当前 `4.5 系统性能指标测试` 开头。

可插入文本：

> 为评价系统交互性能，本文将一次完整语音交互拆分为 ASR 转写、模型推理、TTS 合成、本地播放和状态同步等阶段。设第 \(i\) 次测试的端到端交互时延为 \(T_i\)，测试次数为 \(n\)，则平均时延为：
>
> \[
> \bar{T}=\frac{1}{n}\sum_{i=1}^{n}T_i
> \]
>
> 最大时延为：
>
> \[
> T_{max}=\max(T_1,T_2,\cdots,T_n)
> \]
>
> 对于功能类测试，设成功次数为 \(N_{success}\)，总测试次数为 \(N_{total}\)，则功能成功率定义为：
>
> \[
> R=\frac{N_{success}}{N_{total}}\times 100\%
> \]
>
> 端到端交互时延可进一步拆分为：
>
> \[
> T_{e2e}=T_{ASR}+T_{LLM}+T_{TTS}+T_{play}+T_{sync}
> \]
>
> 其中 \(T_{ASR}\) 表示语音识别耗时，\(T_{LLM}\) 表示模型首字响应耗时，\(T_{TTS}\) 表示语音合成耗时，\(T_{play}\) 表示音频播放耗时，\(T_{sync}\) 表示前后端状态同步耗时。
>
> 对于视觉问答场景，额外引入摄像头快照与图像编码耗时：
>
> \[
> T_{vision}=T_{capture}+T_{encode}+T_{VLM}+T_{TTS}
> \]
>
> 其中 \(T_{capture}\) 表示后端获取最新摄像头帧的耗时，\(T_{encode}\) 表示图像 base64 编码耗时，\(T_{VLM}\) 表示视觉语言模型推理耗时。

### 建议新增测试表

可以在原 `表4-3 系统性能指标记录表` 后增加一个成功率统计表。

表名建议：`表4-4 系统功能成功率统计表`

| 测试项目 | 测试次数 | 成功次数 | 成功率 | 说明 |
|---|---:|---:|---:|---|
| 唤醒词识别 | 20 |  |  | 统计“小灰小灰”等唤醒词是否正确触发 |
| 活跃窗口连续对话 | 20 |  |  | 统计20秒窗口内是否无需重复唤醒 |
| 未唤醒语音拦截 | 20 |  |  | 统计非唤醒语音是否被正确忽略 |
| 视觉问题触发 | 20 |  |  | 统计视觉问题是否附加摄像头快照 |
| 普通问题非触发 | 20 |  |  | 统计普通问题是否避免上传图像 |
| TTS本地播放 | 20 |  |  | 统计回复音频是否正常播放 |
| 麦克风恢复 | 20 |  |  | 统计AI回复后麦克风是否恢复监听 |

如果还没有完整测试数据，可以先保留空白或写“待测”。最终提交前建议至少补一组真实数据。

## 六、建议新增图和表

为了配合新增算法内容，建议新增或调整以下图表。

| 编号建议 | 图表名称 | 放置位置 |
|---|---|---|
| 图3-2 | 语音唤醒门控状态转移图 | 3.3 |
| 图3-3 | 视觉上下文触发流程图 | 3.4 |
| 图3-4 | 表情有限状态机转移图 | 3.5 |
| 图3-5 | DNN+MIL人脸追踪算法流程图 | 3.7 |
| 图3-6 | 人脸水平误差与舵机角度控制示意图 | 3.7 或 3.8 |
| 表3-5 | 人脸追踪与控制参数表 | 3.7 |
| 表4-4 | 系统功能成功率统计表 | 4.5 |

其中，最建议补的是“人脸水平误差与舵机角度控制示意图”。该图可以画成：摄像头画面中间一条中心线，人脸框中心点偏左或偏右，标出 \(c_t\)、\(c_0\)、\(e_t\)，再连接到舵机角度 \(\theta_t\)。

## 七、需要避免的写法

1. 不建议大篇幅推导 Transformer 自注意力：

   \[
   Attention(Q,K,V)=softmax(\frac{QK^T}{\sqrt{d_k}})V
   \]

   这个公式可以在第二章简单提一句，但不要作为论文算法重点。因为本文没有训练或改进 Transformer。

2. 不建议声称“提出了新的视觉语言模型算法”。本文实际贡献是系统集成、触发策略、状态机和嵌入式部署优化。

3. 不建议把关键词视觉触发写成复杂机器学习分类器。当前系统实现是关键词启发式规则，应如实写成轻量触发函数。

4. 不建议回避硬件安全问题。PCA9685 曾经烧毁这件事可以弱化表述为“硬件测试中发现舵机驱动板存在异常发热和损坏风险”，然后强调本文设计了分阶段安全测试流程。

## 八、推荐的论文贡献点改写

当前论文贡献可以在第一章末尾或绪论总结中改成更清晰的三点。

可插入文本：

> 本文的主要工作如下：
>
> （1）设计了一套面向树莓派 kiosk 场景的二次元萌宠具身智能系统架构，将语音唤醒、ASR、视觉语言模型、TTS、本地播放、前端表情和舵机控制接口整合为完整交互闭环。
>
> （2）提出了适用于嵌入式多模态交互终端的轻量化流程控制方法，包括基于时间窗口的语音唤醒门控算法、基于意图关键词的视觉上下文触发策略，以及由后端事件驱动的表情状态机。
>
> （3）设计了基于 DNN 人脸检测与 MIL 跟踪的视觉跟随方案，并建立了人脸水平误差、比例控制、死区处理、最大步长限制和 PCA9685 PWM 输出之间的控制映射关系，为后续实体萌宠动作反馈提供基础。

## 九、推荐优先修改顺序

建议按以下顺序修改，效率最高：

1. 先改第三章 `3.7` 和 `3.8`，加入人脸追踪误差、平滑、死区、控制律和 PWM 映射公式；
2. 再改第三章 `3.3`，加入语音唤醒门控状态模型和伪代码；
3. 再改第三章 `3.4`，加入视觉触发函数和伪代码；
4. 再改第三章 `3.5`，加入表情有限状态机；
5. 最后改第四章 `4.5`，加入平均时延、最大时延、成功率和端到端时延分解公式；
6. 第二章只补少量基础公式，不要喧宾夺主。

## 十、最终效果预期

完成上述修改后，论文会明显具备以下特点：

1. 有明确的问题建模：唤醒、视觉触发、表情和舵机控制都有形式化描述；
2. 有算法表达：第三章不再只是工程流程说明，而是包含伪代码和状态机；
3. 有控制公式：人脸误差到舵机角度之间存在清晰映射；
4. 有实验指标：第四章测试结果可以用平均值、最大值和成功率解释；
5. 答辩更稳：老师问“你的算法在哪里”时，可以指向语音门控、视觉触发、人脸追踪和舵机控制四个部分。

